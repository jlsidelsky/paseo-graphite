{
  "name": "paseo-graphite-pr-tags",
  "private": true,
  "version": "0.1.0",
  "description": "Paseo plugin: tags each session with the PRs it works on, for any provider.",
  "type": "module",
  "files": ["paseo-plugin.json", "index.server.ts", "server/"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@getpaseo/client": "0.9.1"
  },
  "devDependencies": {
    "@getpaseo/plugin": "0.9.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
